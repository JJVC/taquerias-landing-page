---
layout: layouts/base.njk
permalink: "/"
---
{% set data = taquerias.gaviotas %}
{% set seoData = data.seo %}
{% set heroImage = data.assets.hero %}
{% set whatsappNumber = data.contacto.whatsapp | replace('+', '') %}
{# {% set whatsappMsg = (data.mensajeWhatsApp ~ ' - Cupón: PLACEHOLDER') | urlencode %} #}
{% set whatsappMsg = (data.mensajeWhatsApp) | urlencode %}

<!-- Hero Section -->
<section class="hero" style="background-image: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.7)), url('{{ data.assets.hero }}');">
  <div class="container">
    <div class="hero-content">
      <img src="{{ data.assets.logo }}" alt="{{ data.nombre }} Logo" class="hero-logo">
      <!--h1 class="hero-title">
        <span class="taqueria-label">Taquería</span>
        <span class="taqueria-name">{{ data.nombre | replace('Taquería ', '') }}</span>
      </h1>
      <p class="hero-lema">{{ data.lema }}</p-->
      
      <!-- Diferenciadires Rápidos -->
      <div class="hero-badges">
        {% for badge in data.diferenciadoresUnicos %}
        <span class="badge">✓ {{ badge }}</span>
        {% endfor %}
      </div>
      
      <!-- CTA Principal -->
      <div class="hero-cta">
        <a href="#promociones"
           class="btn btn-primary btn-lg"
           target="_blank">
          🌮 Haz tu Pedido Ahora
        </a>
        <p class="cta-urgencia">⚡ Abierto hasta las 2:00 AM</p>
      </div>
    </div>
  </div>
  
  <!-- Scroll Indicator -->
  <div class="scroll-indicator">
    <a href="#promociones">Desliza para ver PROMOCIONES</a>
    <div class="scroll-arrow">↓</div>
  </div>
</section>

<!-- Promoción Destacada Section -->
<!--section class="promo-destacada-temporal" 
         data-fecha-inicio="{{ data.promociones.temporal.fechaInicio }}" 
         data-fecha-fin="{{ data.promociones.temporal.fechaFin }}"
         style="padding: var(--spacing-md) 0; background-color: var(--color-fondo);">
  <div class="container">
    <div class="promo-box">
      <div class="promo-icon">🎁</div>
      <div class="promo-content">
        <strong>PROMOCIÓN EXCLUSIVA:</strong>
        <p>{{ data.promociones.cupon.descripcion }}</p>
        <span class="promo-code">Cupón: {{ data.promociones.cupon.codigo }}</span>
      </div>
    </div>
  </div>
</section-->

<!-- Especialidades Section -->
<section class="especialidades" id="menu">
  <div class="container">
    <h2 class="section-title">Tacos</h2>
    <p class="section-subtitle">Carne de calidad premium • Receta familiar de +25 años</p>
    
    <div class="especialidades-grid">
      {% for item in data.especialidades %}
      <div class="especialidad-card {% if item.destacado %}destacado{% endif %}">
        {% if item.destacado %}
        <div class="destacado-badge">🔥 MÁS VENDIDO</div>
        {% endif %}
        <div class="card-image" style="background-image: url('{{ item.imagen }}');"></div>
        <div class="card-content">
          <h3>{{ item.nombre }}</h3>
          <p>{{ item.descripcion }}</p>
          <a href="https://wa.me/{{ whatsappNumber }}?text=Hola! Quiero ordenar {{ item.nombre }} 🌮" 
             class="btn btn-secondary"
             target="_blank">
            Ordenar Ahora
          </a>
        </div>
      </div>
      {% endfor %}
    </div>
    
    <!-- Ver Menú Completo -->
    <div class="menu-completo">
      <h3>¿Quieres ver todo el menú?</h3>
      <a href="https://wa.me/{{ whatsappNumber }}?text=Hola! Me puede mandar su menú 🌮" 
        class="btn btn-secondary"
        target="_blank">
        Pedir menú
      </a>
    </div>
  </div>
</section>

<!-- Proof Section (Prueba Social) -->
<!--section class="proof-section">
  <div class="container">
    <div class="proof-content">
      <div class="proof-stat">
        <span class="stat-number">+25</span>
        <span class="stat-label">Años de Experiencia</span>
      </div>
      <div class="proof-stat">
        <span class="stat-number">⭐⭐⭐⭐⭐</span>
        <span class="stat-label">Calificación de Clientes</span>
      </div>
      <div class="proof-stat">
        <span class="stat-number">100%</span>
        <span class="stat-label">Carne de Calidad</span>
      </div>
    </div>
  </div>
</section-->

<!-- Promociones Section -->
<section class="promociones"
  id="promociones"
        data-fecha-inicio="{{ data.promociones.temporal.fechaInicio }}" 
        data-fecha-fin="{{ data.promociones.temporal.fechaFin }}">
  <div class="container">
    <h2 class="section-title">Aprovecha Nuestras Promociones</h2>
    
    <div class="promociones-grid">
      {% for promo in data.promociones.lista %}
      <div class="promo-card promo-cortesia">
        <div class="promo-header">
          <span class="promo-emoji">{{ promo.icono }}</span>
          <h3>{{ promo.titulo }}</h3>
        </div>
        <p>{{ promo.descripcion }}</p>
        <p class="promo-minimo">Por: <strong>${{ promo.monto }}</strong></p>
        <p class="promo-acciones-label">Selecciona tu bebida y utiliza el cupón:</p>
        <div class="promo-actions">
          {% for action in promo.action %}
           <a href="https://wa.me/{{ whatsappNumber }}?text={{ (action.messageWhatsApp ~ ' Cupón: PLACEHOLDER') | urlencode }}" 
             class="btn btn-secondary"
             target="_blank">
            <svg class="whatsapp-icon" viewBox="0 0 32 32" fill="currentColor" aria-hidden="true">
            <path d="M16 0C7.164 0 0 7.164 0 16c0 2.829.744 5.482 2.038 7.782L0 32l8.4-2.02C10.652 31.248 13.248 32 16 32c8.836 0 16-7.164 16-16S24.836 0 16 0zm0 29.333c-2.547 0-4.924-.714-6.951-1.951l-.498-.293-5.16 1.244 1.271-5.026-.322-.516C2.755 20.764 2 18.458 2 16 2 8.28 8.28 2 16 2s14 6.28 14 14-6.28 13.333-14 13.333zm7.747-10.027c-.425-.213-2.516-1.24-2.907-1.382-.391-.142-.676-.213-.96.213-.285.425-1.102 1.382-1.351 1.667-.249.284-.498.32-.924.107-.425-.213-1.795-.662-3.418-2.109-1.264-1.127-2.116-2.518-2.364-2.942-.249-.425-.027-.655.186-.867.191-.191.425-.498.638-.747.213-.249.284-.425.425-.711.142-.284.071-.533-.036-.747-.107-.213-.96-2.311-1.316-3.164-.347-.831-.698-.718-.96-.731-.249-.013-.533-.016-.818-.016s-.747.107-1.138.533c-.391.425-1.493 1.458-1.493 3.555s1.529 4.125 1.742 4.409c.213.285 3.004 4.582 7.276 6.427 1.016.44 1.811.702 2.431.898.102.031 1.955.658 2.24.818.711.427 1.529.355 2.107.213.64-.16 2.516-1.029 2.871-2.022.355-.993.355-1.844.249-2.022-.107-.178-.391-.284-.818-.498z"/>
            </svg> {{ action.nombre }} 🎁
          </a>
          {% endfor %}
        </div>
      </div>
      {% endfor %}
      
    </div>
    
    <p class="promociones-nota">⚠️ {{ data.promociones.nota }}</p>
  </div>
</section>

<!-- Galería del Local -->
<section class="galeria-section">
  <div class="container">
    <h2 class="section-title">Conoce Nuestro Local</h2>
    <p class="section-subtitle">Un ambiente familiar frente a la U.H. Gaviotas de {{ data.contacto.ubicacion.colonia }}</p>
    
    <div class="galeria-content">
      <div class="galeria-image">
        <img src="{{ data.assets.fachada }}" alt="Fachada de {{ data.nombre }}" loading="lazy">
      </div>
      <div class="galeria-text">
        <h3>📍 Nos encontramos en {{ data.contacto.ubicacion.colonia }}</h3>
        <p>Un local acogedor donde la tradición y el sabor se encuentran. Ven a disfrutar de nuestros tacos en un ambiente familiar.</p>
        <ul class="galeria-features">
          <li>✓ Espacio limpio y cómodo</li>
          <li>✓ Atención personalizada</li>
          <li>✓ Preparación a la vista</li>
          <li>✓ Para comer aquí o llevar</li>
        </ul>
        <a href="{{ data.contacto.ubicacion.googleMapsUrl }}" 
           target="_blank"
           class="btn btn-primary">
          📍 Cómo Llegar
        </a>
      </div>
    </div>
  </div>
</section>

<!-- Horarios y Ubicación -->
<section class="info-section">
  <div class="container">
    <div class="info-grid">
      <!-- Horarios -->
      <div class="info-card">
        <div class="info-icon">🕒</div>
        <h3>Horarios</h3>
        <p class="info-highlight">{{ data.horarios.texto }}</p>
        <p class="info-detail">¡Abierto todos los días para ti!</p>
      </div>
      
      <!-- Ubicación -->
      <div class="info-card">
        <div class="info-icon">📍</div>
        <h3>Ubicación</h3>
        <p class="info-highlight">{{ data.contacto.ubicacion.colonia }}, {{ data.contacto.ubicacion.ciudad }}</p>
        <a href="{{ data.contacto.ubicacion.googleMapsUrl }}" 
           target="_blank"
           class="btn btn-outline">
          Ver en Google Maps
        </a>
      </div>
      
      <!-- WhatsApp -->
      <div class="info-card">
        <div class="info-icon">📱</div>
        <h3>WhatsApp</h3>
        <p class="info-highlight">{{ data.contacto.whatsappDisplay }}</p>
        <a href="#promociones" 
           target="_blank"
           class="btn btn-whatsapp">
          Enviar Mensaje
        </a>
      </div>
    </div>
  </div>
</section>

<!-- Mapa -->
<section class="mapa-section">
  <div class="container">
    <h2 class="section-title">Encuéntranos Aquí</h2>
    <div class="mapa-container">
      <iframe 
        src="{{ data.contacto.ubicacion.googleMapsEmbed }}"
        width="100%" 
        height="450" 
        style="border:0;" 
        allowfullscreen="" 
        loading="lazy" 
        referrerpolicy="no-referrer-when-downgrade">
      </iframe>
    </div>
  </div>
</section>

<!-- CTA Final -->
<section class="cta-final">
  <div class="container">
    <h2>¿Listo para Probar el Mejor Pastor de Acapulco?</h2>
    <p>Haz tu pedido ahora</p>
    <a href="#promociones" 
       class="btn btn-primary btn-xl pulse"
       target="_blank">
      🌮 Ordenar por WhatsApp
    </a>
    <p class="cta-confianza">✓ Pago contra entrega • ✓ Sin costo de envío • ✓ Respuesta inmediata</p>
  </div>
</section>

<!-- Footer -->
<footer class="footer">
  <div class="container">
    <img src="{{ data.assets.logo }}" alt="{{ data.nombre }}" class="footer-logo">
    <p>&copy; 2026 {{ data.nombre }}. Todos los derechos reservados.</p>
    <p class="footer-location">{{ data.contacto.ubicacion.direccion }}</p>
    <div class="footer-social">
      <a href="https://wa.me/{{ whatsappNumber }}" target="_blank">WhatsApp</a>
    </div>
  </div>
</footer>
